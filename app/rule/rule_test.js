/**
 * Created by henry on 26/10/2015.
 */

'use strict';

describe('myApp.rule module', function() {

    beforeEach(module('myApp.rule'));

    describe('rule controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var ruleCtrl = $controller('RuleCtrl');
            expect(ruleCtrl).toBeDefined();
        }));

    });
});