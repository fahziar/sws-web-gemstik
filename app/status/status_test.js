/**
 * Created by henry on 25/10/2015.
 */

'use strict';

describe('myApp.status module', function() {

    beforeEach(module('myApp.status'));

    describe('status controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var statusCtrl = $controller('StatusCtrl');
            expect(statusCtrl).toBeDefined();
        }));

    });
});